# Supernote Apple Note Unifier - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Database password for Supernote Personal Cloud MariaDB
# Required if using Personal Cloud sync (to register files in database)
SUPERNOTE_DB_PASSWORD=your-password-here

# =============================================================================
# OPTIONAL SETTINGS (defaults shown)
# =============================================================================

# Database connection mode: "docker" or "tcp"
# - docker: Connect via docker exec (default)
# - tcp: Connect directly via TCP/IP
# SUPERNOTE_DB_MODE=docker

# Docker container name (for docker mode)
# SUPERNOTE_DOCKER_CONTAINER=supernote-mariadb

# Database connection settings (for tcp mode)
# SUPERNOTE_DB_HOST=localhost
# SUPERNOTE_DB_PORT=3306

# Database credentials
# SUPERNOTE_DB_USER=supernote
# SUPERNOTE_DB_NAME=supernotedb

# =============================================================================
# PATH SETTINGS (defaults shown)
# =============================================================================

# Supernote storage mount point
# SUPERNOTE_MOUNT_PATH=/Volumes/Storage/Supernote

# Local state database path
# UNIFIER_STATE_DB=~/.local/share/supernote-unifier/state.db

# Backup directory for Apple Notes
# UNIFIER_BACKUP_DIR=~/.local/share/supernote-unifier/backups

# Log directory
# UNIFIER_LOG_DIR=~/.local/share/supernote-unifier/logs
